import { px } from "@zos/utils";
import { DEVICE_WIDTH } from "../../utils/config/device";

export const LAYOUT = {
  W: DEVICE_WIDTH,
  statusBarH: px(68),
  headerH: px(70),
  btnH: px(88),
  pad: px(32),
};
